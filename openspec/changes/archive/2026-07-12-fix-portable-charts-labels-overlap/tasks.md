## 1. Implementation

- [x] 1.1 Implement collision detection for the frequency label (`centerText`) in the horizontal bar charts' `barLabels` plugin in `app.js`.
- [x] 1.2 Dynamically render `centerText` outside using the `nextX` cursor if it overlaps the score.

## 2. Verification

- [x] 2.1 Verify that Notebook, Handheld, and SBC charts render score, frequency, and contributor names without overlap.
