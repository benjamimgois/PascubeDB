## 1. Implementation

- [x] 1.1 Verify argument alignment of `gpuChart` rendering call in `app.js` and verify it passes `gpuRuns.map(r => r.gpuMaxFreq)` as the `gpuFreqs` argument.

## 2. Verification

- [x] 2.1 Verify that the Top 10 GPU Model Performance chart displays "FREQ MHz" centered in its bars (or next to them if they are narrow) in a small, non-bold font.
