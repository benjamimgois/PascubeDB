## 1. Implementation

- [x] 1.1 Update `renderHorizontalBarChart` in `app.js` with dynamic `nextX` cursor logic for narrow horizontal bar labels.
- [x] 1.2 Unify the client/contributor rendering logic to use the computed `nextX` cursor.

## 2. Verification

- [x] 2.1 Manually verify that labels in the GPU Thermal Efficiency chart do not overlap on narrow bars.
- [x] 2.2 Verify that labels still render centered and correctly on wide bars.
