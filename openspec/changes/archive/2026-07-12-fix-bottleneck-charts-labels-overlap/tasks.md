## 1. Implementation

- [x] 1.1 Update the custom `ratioPlugin` in `app.js` with dynamic `nextX` cursor logic for narrow bars to avoid overlaps.

## 2. Verification

- [x] 2.1 Verify that both CPU vs GPU Bottleneck charts render labels without overlap even on narrow bars.
