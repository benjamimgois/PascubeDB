## 1. Implementation

- [x] 1.1 Update `centerText` construction in `app.js` to combine both CPU and GPU max frequencies when available.

## 2. Verification

- [x] 2.1 Verify that the Top 10 Benchmark Runs chart displays "CPU_FREQ / GPU_FREQ MHz" centered in its bars (or next to them if they are narrow) in a small, non-bold font.
