## 1. Implementation

- [x] 1.1 Update `STATS_PILL_LABELS.efficiency` in `app.js` to change "Major CPU Bottleneck" to "TOP CPU Bottleneck".
- [x] 1.2 Update the card and podium value formatting logic in `app.js` inside `renderStats` (efficiency block) to append "Pts / MHz" and "Pts / ºC".

## 2. Verification

- [x] 2.1 Verify that the Efficiency tab cards and podiums display the correct unit labels and card title.
